import {FC} from "react";

interface WatchAllDevicesProps{
  imageUrl: string
}

const WatchAllDevices:FC<WatchAllDevicesProps> = ({imageUrl}) =>{
  return(
    <div>

    </div>
  )
}

export default WatchAllDevices;
